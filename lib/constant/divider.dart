import 'package:flutter/material.dart';

import 'color.dart';

const dividerI76 = Divider(height: 1, color: underLineColor,indent: 76);
const defaultDivider = Divider(height: 0, color: underLineColor);